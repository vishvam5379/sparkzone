{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python create_superuser.py && python seed_data.py && daphne -b 0.0.0.0 -p $PORT sparkzoneproject.asgi:application"
  }
}
